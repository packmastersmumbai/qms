# OS
.DS_Store
Thumbs.db
*.url

# Credentials / local config — never commit
.env
*.credentials.json
creds.json
client_secret*.json

# Large media
*.jpg
*.jpeg
*.png
*.mp4
*.avi

# Docs / working files (not source code)
docs/

# clasp auth (global, not project-specific)
# .clasp.json is intentionally included for CI/CD

# Editor / tooling state — never commit
.claude/
.playwright-cli/

# File backups (pre-edit snapshots)
*.bak

# Playwright accessibility-tree dumps (not source)
landing-post-deploy.yml
*-accessibility.yml
