# Architecture Scratchpad: Warehouse Floorplan & Location Module

Task: .specs/tasks/draft/implement-warehouse-floorplan-location-module.feature.md
Skill: .claude/skills/gas-warehouse-floorplan/SKILL.md
Analysis: .specs/analysis/analysis-warehouse-floorplan.md

## Problem Decomposition
1 requirements (4 goals, P1/P2) -> 2 patterns (pageMap, _F.html templates, Warehouse.js backend, seed) -> 3 approaches -> 4 decision (visual layer over verbatim backend) -> 5 components (WarehouseFloorplan.html; seed extend; P2 geometry+suggestSlot) -> {6 integration, 7 dataflow} -> 8 build seq. P2 after P1; saveMaterial-fix before fields before suggestSlot.

## Architecture Pattern
Layered read-only presentation over existing service layer. Precedent: Landing.html->getLandingBundleV3Fast; Records_F.html->getRecordsList. No backend re-architecture.

## Reusable Code (analysis "Verified backend")
getStockView(306), getStockSummary(176), getLowStockItems(199), getLocations(278), whereIsLot(Scan.js:415), recordLocationTransfer, issueRMForProduction -> all use-as-is. LOCATIONS_SEED(Initialize.js:107) EXTEND. saveMaterial(Masters.js:350) ADAPT 5->12 col. inferLocType(Warehouse.js:329) ADAPT floor-letter fallback. New: WarehouseFloorplan.html, suggestSlot(P2).

## Decision + RISK bake-in
- RISK-1: every B### seed row sets col I Type (A=RM,B/C/D=PM,E/F=FG) AND extend inferLocType floor-letter fallback (defence-in-depth so getStockView colouring never ''}.
- RISK-2: reseed ADDS B001-B148, KEEPS 8 legacy zones (IQC/OQC/NCR/CustomerReturn/Rework/_J07 + 3 Scan chokepoints). Init:552 reset re-runs -> idempotent, both coexist. Never delete zones.
- RISK-3: saveMaterial -> read-modify-write 12-col by-index writer BEFORE F->L fields; getMaterials widened. P2 gate.
- Tile-pick -> recordLocationTransfer (recordScan rejects non-chokepoint B### IDs).

## Data Flow
Read: LOCATIONS+STOCK_LEDGER->getStockView->{id->occ}->heatmap rect fill; getStockSummary/getLowStockItems->KPI. Tap->getStockView row->PIP. Search->whereIsLot->highlight.
Write: tap+confirm->recordLocationTransfer(lot,from,to,qty)->STOCK_LEDGER->re-read.
P2: getMaterials geom+qty->suggestSlot walks getLocations open slots, min(volume,weight), palletsNeeded=ceil(qty/perPallet), prefer consolidating->Accept->recordLocationTransfer.

## Sections selected (complexity L)
Solution Strategy Y; Architecture Decisions Y; Decomposition Y; Building Block View Y; Runtime Scenarios Y; Data Flow folded Y; Expected Changes Y; Contracts Y; Workflow/High-Level N.

## Self-Critique
Decomposition present; P2 gated on P1 + saveMaterial gates fields; pattern matches _F.html precedent; single decisive approach; file+line refs present; build-seq deps correct; DDD one-way presentation->service, no generic names; every component cites reuse or justifies new. RISK-1/2/3 + chokepoint baked. PASS.
