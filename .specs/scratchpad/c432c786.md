# Scratchpad: Warehouse Floorplan Impact Analysis

Task: .specs/tasks/draft/implement-warehouse-floorplan-location-module.feature.md
Created: 2026-07-05

## Verified evidence (file:line)

### Warehouse.js
- getLocations (278): reads LOCATIONS cols 0-11; filter `r[0] && r[11]!=='N'`; typeFilter matches `String(r[8]).toUpperCase()`. Object: id r[0], floor r[1], section r[2], aisle r[3], rack r[4], shelf r[5], bin r[6], label r[7]||r[0], type r[8], capacityQty r[9], capacityUnit r[10], active r[11]. => capacityQty/Unit READ into object but never consumed by any caller. CONFIRMED unused.
- getOpenRMLocations (296) = getLocations('RM')+getLocations('QUARANTINE')
- getFGLocations (300) = getLocations('FG')+getLocations('FG_HOLD')
- getStockView (306): builds locTypeMap from LOCATIONS col r[8]; inferLocType fallback (329-341).
- inferLocType (329): QUAR/SCRAP/SAMPLE/REWORK/WIP/FG-HOLD, then startsWith('FG')->FG, startsWith('RM')->RM, else ''. *** B001 IDs => '' unknown type. NEW RISK not in task. ***
- getStockSummary (~150) scalar rollup; getStockByMaterial (176); getLowStockItems (199) reuse summary.
- getFIFOLots (239) reads LOCATIONS col r[8] type.
- saveLocation (523): writes 12-col row incl d.capacityQty/Unit (537,547).
- deleteLocation (554): blocks delete if referenced in ledger.

### Scan.js
- recordScan (289): validates locationId against CHOKEPOINTS_ (4 chokepoints only) — NOT arbitrary LOCATIONS ids. verb/action/target from CHOKEPOINT_* maps.
- CHOKEPOINT_LOCATION_ (48): GATE-IN->SCAN-GATE-IN, FLOOR-1->SCAN-FLOOR-1, FLOOR-2->SCAN-FLOOR-2, GATE-OUT->DISPATCHED.
- CHOKEPOINT_LOCATION_ROWS_ (56): 3 extra LOCATIONS rows appended by ensureChokepointLocations_ (67), types RM/WIP.
- whereIsLot (415): latest SCAN_EVENTS row -> locationId latest[4].
- lookupLotForScan (217): source loc = loc holding most of lot (from getStockSummary).
- Location from scan resolved via chokepoint sticker, not free tile pick.

### Initialize.js
- LOCATIONS_HEADERS (102): 12 cols.
- LOCATIONS_SEED (107): 8 rows — RM-STORE-A, RM-STORE-B, QUARANTINE, FG-STORE, FG-HOLD, SCRAP-AREA, SAMPLE-CABINET, REWORK-AREA. (task said "8 logical zones" — confirmed, though only 5 grepped by first pattern; sed shows all 8.)
- Used at 260 (createMasterSheet_) and 552 (reset). +3 chokepoint rows from Scan.js at runtime.

### Masters.js
- getMaterials (14): reads cols A-E; returns code r[0], desc? , category r[3], defaultLocation r[4]. Only 5 cols.
- saveMaterial material branch (350): `row = [data.code, data.desc, data.unit, data.category, data.defaultLocation||'']` — FIXED 5 elements. setValues at 354 / appendRow 358 with row.length=5. *** TRUNCATES F->L on every edit. CONFIRMED. ***
- ensureMaterialsLocationColumn_ (75): only ensures col E header exists.

### Code.js
- getSpreadsheet (10). doGet (219). pageMap (270-ish) canonical lowercase keys -> {file,title}. Includes `warehouse: Warehouse_F`. New page = add pageMap entry + create *_F.html. ADMIN_PAGES_ guards settings/masterscrud.
- include (310) = createHtmlOutputFromFile. computePendingCounts_ (407) module badges — floorplan is not a pending-count module.
- Default homepage = QMSV2_F cockpit; nav tiles there.

### Hardcoded location-ID readers (break if 8 zones removed)
- IQC.js:174-179,644 'QUARANTINE'
- OQC.js:296-304 'FG-HOLD','QUARANTINE'
- NCR.js:118 'FG-HOLD','QUARANTINE'
- CustomerReturn.js:230,383,385,402,427 'QUARANTINE','FG-STORE'
- Rework.js:136 'FG-STORE','RM-STORE-A'
- _J07.js:15 'FG-STORE'; _Smoke*/_ProductionDiag* test refs.
=> Reseed must ADD B001-B148, KEEP the 8 zones. Do not replace.

### GRN wiring
- GRN.js:7 getOpenRMLocations; :59 getLocations('RM'); :97 per-item loc = item.locationId || defaultLocation.
- GRN_F.html uses m.defaultLocation for putaway location. Picker = material default, not tile map.
